{
  "scripts": {
    "test": "mocha",
    "coverage": "nyc --reporter=text-summary --reporter=html --check-coverage --lines=90 --per-file mocha",
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "joi": "^17.4.2"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "nyc": "^15.1.0"
  }
}
